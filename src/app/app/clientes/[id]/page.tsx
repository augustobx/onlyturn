import Link from "next/link";
import { notFound } from "next/navigation";
import { formatInTimeZone } from "date-fns-tz";
import { addCustomerLedgerEntryAction, updateCustomerAccountStatusAction } from "@/app/actions/customers";
import { requireTenantSession } from "@/lib/auth";
import { createTenantDb } from "@/lib/tenant-db";

const accountLabels={PENDING:"Pendiente de aprobación",ACTIVE:"Cuenta activa",REJECTED:"Cuenta rechazada",SUSPENDED:"Cuenta suspendida"} as const;const ledgerLabels={CHARGE:"Cargo",PAYMENT:"Pago",CREDIT:"Crédito",ADJUSTMENT:"Ajuste"} as const;
export default async function CustomerDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const {membership,tenant}=await requireTenantSession();const customer=await createTenantDb(membership.tenantId).customerDetail(id);if(!customer)notFound();const balance=customer.ledgerEntries.reduce((sum,item)=>sum+item.amountCents,0);const money=new Intl.NumberFormat("es-AR",{style:"currency",currency:tenant.currency});return <><div className="page-title"><span className="eyebrow">Ficha del cliente</span><h1>{customer.firstName} {customer.lastName}</h1><p className="muted">{customer.phone} · {customer.email??"Sin email"}</p></div><div className="settings-actions" style={{marginBottom:18}}><Link className="button ghost" href="/app/clientes">← Volver a clientes</Link></div><div className="customer-admin-summary"><section className="card"><span className="eyebrow">Cuenta registrada</span>{customer.account?<><h2>{accountLabels[customer.account.status]}</h2><p className="muted">{customer.account.email}</p><div className="settings-actions">{customer.account.status!=="ACTIVE"&&<StatusButton customerId={id} status="ACTIVE" label="Aprobar y activar"/>}{customer.account.status!=="SUSPENDED"&&customer.account.status!=="PENDING"&&<StatusButton customerId={id} status="SUSPENDED" label="Suspender" secondary/>}{customer.account.status==="PENDING"&&<StatusButton customerId={id} status="REJECTED" label="Rechazar" secondary/>}</div></>:<div className="empty">Este cliente todavía no creó una cuenta.</div>}</section><section className="card"><span className="eyebrow">Cuenta corriente</span><strong className={balance>0?"account-balance due":"account-balance"}>{money.format(balance/100)}</strong><p className="muted">{balance>0?"Saldo pendiente":balance<0?"Saldo a favor":"Cuenta al día"}</p></section><section className="card"><span className="eyebrow">Historial</span><strong className="account-balance">{customer.bookings.length}</strong><p className="muted">turnos registrados</p></section></div><div className="grid two-col customer-admin-columns"><section className="card"><h2>Agregar movimiento</h2><p className="muted">Los cargos aumentan la deuda; pagos y créditos la reducen.</p><form action={addCustomerLedgerEntryAction}><input type="hidden" name="customerId" value={id}/><div className="field"><label>Tipo</label><select className="select" name="type"><option value="CHARGE">Cargo</option><option value="PAYMENT">Pago recibido</option><option value="CREDIT">Crédito a favor</option><option value="ADJUSTMENT">Ajuste con signo</option></select></div><div className="field"><label>Importe</label><input className="input" name="amount" type="number" step="0.01" required/></div><div className="field"><label>Concepto</label><input className="input" name="description" maxLength={200} required placeholder="Ej. Saldo de sesión"/></div><div className="field"><label>Vincular a turno (opcional)</label><select className="select" name="bookingId"><option value="">Sin turno asociado</option>{customer.bookings.map(item=><option value={item.id} key={item.id}>{formatInTimeZone(item.startsAt,tenant.timezone,"dd/MM/yy HH:mm")} · {item.service.name}</option>)}</select></div><button className="button">Guardar movimiento</button></form></section><section className="card"><h2>Historial de turnos</h2>{customer.bookings.length?<div className="account-history">{customer.bookings.map(item=><article key={item.id}><div><strong>{item.service.name}</strong><small>{item.location.name}{item.professional?` · ${item.professional.name}`:""}</small></div><div><span className={`status ${item.status}`}>{item.status}</span><small>{formatInTimeZone(item.startsAt,tenant.timezone,"dd/MM/yyyy HH:mm")}</small></div></article>)}</div>:<div className="empty">No hay turnos.</div>}</section></div><section className="card account-ledger"><h2>Movimientos</h2>{customer.ledgerEntries.length?<div className="table-wrap"><table className="table"><thead><tr><th>Fecha</th><th>Concepto</th><th>Tipo</th><th>Importe</th></tr></thead><tbody>{customer.ledgerEntries.map(item=><tr key={item.id}><td>{formatInTimeZone(item.createdAt,tenant.timezone,"dd/MM/yyyy HH:mm")}</td><td>{item.description}</td><td>{ledgerLabels[item.type]}</td><td className={item.amountCents>0?"ledger-debit":"ledger-credit"}>{money.format(item.amountCents/100)}</td></tr>)}</tbody></table></div>:<div className="empty">No hay movimientos.</div>}</section></>}

function StatusButton({customerId,status,label,secondary=false}:{customerId:string;status:"ACTIVE"|"REJECTED"|"SUSPENDED";label:string;secondary?:boolean}){return <form action={updateCustomerAccountStatusAction}><input type="hidden" name="customerId" value={customerId}/><input type="hidden" name="status" value={status}/><button className={`button${secondary?" secondary":""}`}>{label}</button></form>}
