import Link from "next/link";
import { requireTenantSession } from "@/lib/auth";
import { createTenantDb } from "@/lib/tenant-db";

const accountLabels={PENDING:"Pendiente",ACTIVE:"Activa",REJECTED:"Rechazada",SUSPENDED:"Suspendida"} as const;
export default async function CustomersPage(){const {membership,tenant}=await requireTenantSession();const rows=await createTenantDb(membership.tenantId).customers();const money=new Intl.NumberFormat("es-AR",{style:"currency",currency:tenant.currency});const pending=rows.filter(item=>item.account?.status==="PENDING").length;return <><div className="page-title"><span className="eyebrow">Relaciones</span><h1>Clientes</h1><p className="muted">Perfiles, cuentas registradas, historial y saldos dentro de esta organización.</p></div>{pending>0&&<div className="account-notice"><strong>{pending} registro{pending===1?"":"s"} pendiente{pending===1?"":"s"}</strong> · Revisalos desde la ficha del cliente.</div>}<div className="card table-wrap"><table className="table"><thead><tr><th>Cliente</th><th>Contacto</th><th>Cuenta</th><th>Turnos</th><th>Saldo</th><th></th></tr></thead><tbody>{rows.map(customer=>{const balance=customer.ledgerEntries.reduce((sum,item)=>sum+item.amountCents,0);return <tr key={customer.id}><td><strong>{customer.firstName} {customer.lastName}</strong><br/><small className="muted">Actualizado {new Intl.DateTimeFormat("es-AR").format(customer.updatedAt)}</small></td><td>{customer.phone}<br/><small>{customer.email??"Sin email"}</small></td><td>{customer.account?<span className={`status account-${customer.account.status.toLowerCase()}`}>{accountLabels[customer.account.status]}</span>:<span className="muted">Sin registro</span>}</td><td>{customer._count.bookings}</td><td className={balance>0?"ledger-debit":"ledger-credit"}>{money.format(balance/100)}</td><td><Link className="button secondary" href={`/app/clientes/${customer.id}`}>Ver ficha</Link></td></tr>})}</tbody></table></div></>}
